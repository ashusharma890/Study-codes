class Compute
{
    String average(int A[], int N)
    {
        double sum=0;
        double avg=0;
     for(int i=0;i<N;i++)
     {
         sum+=A[i];
          avg=(double) sum/N;
     } 
     return String.format("%.2f",avg);
    }
}
